/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",  // Include all JS/TSX files in src
  ],
  theme: {
    extend: {},                   // Extend Tailwind’s default theme if needed
  },
  plugins: [],                    // Add Tailwind plugins here if any
}
